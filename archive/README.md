# Archive

Superseded drafts from the first pass at Batch 1 (multi-role `user_roles`
table, admin role screen). Kept only because file deletion needed your
approval. Nothing here is built, linted, tested or applied to the database.

Safe to delete: `git rm -r archive && git commit -m "Remove archived drafts"`
